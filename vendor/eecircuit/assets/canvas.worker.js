/* Stub. The original file here is part of eecircuit-schematic, which is
   distributed obfuscated and without an open-source licence, so it is not
   redistributed in this deployment — see PROVENANCE.md. The schematic
   feature is disabled; simulation and plotting are unaffected. */
self.onmessage = () => {};
